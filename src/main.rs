// use regex::Regex;

// use std::collections::HashMap;
fn main() {
    // let regexp = Regex::new("foo.*").unwrap();
    println!("Hello, world!");
    // let foo = if let Ok(it) = Regex::new("foo.*") {
    //     it
    // } else {
    //     todo!()
    // };
}
